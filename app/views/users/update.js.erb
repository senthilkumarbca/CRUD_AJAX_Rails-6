<% if @user.errors.any? %>
  $('#errorSection').html("<%= escape_javascript render('errors', user: @user) %>");
<% else %>
  $('#user_<%= @user.id %>').replaceWith("<%= escape_javascript render('userRow', u: @user) %>");
  $('.modal-close').click();
<% end %>