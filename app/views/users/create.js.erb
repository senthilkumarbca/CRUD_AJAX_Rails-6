<% if @user.errors.any? %>
  $('#errorSection').html("<%= escape_javascript render('errors', user: @user) %>");
<% else %>
  $('#usersTable').prepend("<%= escape_javascript render('userRow', u: @user) %>");
  $('.modal-close').click();
<% end %>